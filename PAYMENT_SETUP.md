# 파이커피 토스페이먼츠 설정 인계

이 문서는 주문·결제 코드가 GitHub에 반영된 뒤 사장님 노트북에서 진행할
외부 설정 순서를 정리한다.

## 중요한 원칙

- 사업자등록증과 정산계좌번호는 토스페이먼츠 공식 화면에만 입력한다.
- 토스 시크릿 키와 Supabase 서비스 역할 키는 Vercel 환경변수에만 입력한다.
- 비밀키, 계좌번호, 인증번호를 채팅·GitHub·코드·캡처에 남기지 않는다.
- 테스트 결제 검증 전에는 `CHECKOUT_ENABLED=false`를 유지한다.
- 라이브 전환 최종 승인 전에는 `ALLOW_LIVE_PAYMENTS=false`를 유지한다.

## 확정 운영 정책

아래 항목은 별도 질문 없이 초기 운영 기준으로 적용한다.

- 기본 배송비: 3,000원
- 제주 추가 배송비: 3,000원
- 도서산간 추가 배송비: 5,000원
- 출고: 결제 확인 후 영업일 기준 1~3일 이내
- 구매 자격: 로그인한 회원만 주문 가능
- 배송메모: 자주 쓰는 문구 선택과 직접 입력 제공
- 단순 변심 교환·반품: 상품 수령 후 7일 이내, 왕복 배송비 6,000원 고객 부담
- 상품 하자·오배송: 상품 수령 후 3개월 이내이면서 그 사실을 안 날부터
  30일 이내, 판매자 배송비 부담
- 개봉·섭취·사용 또는 고객 책임의 훼손으로 상품 가치가 현저히 감소한 경우는
  관계 법령에 따라 교환·반품이 제한될 수 있음
- 반품지: 홈페이지에 표시된 사업장 주소
- 초기 결제수단: 신용·체크카드와 간편결제
- 간편결제 표시 우선순위: 네이버페이, 카카오페이, 토스페이

사장님 확인이 필요한 내부 결정은 아래 두 가지뿐이다.

1. 통신판매업 신고번호 또는 현재 진행 상태
2. 무료배송 기준 금액, 또는 무료배송 없음

## 1. GitHub Desktop

사장님 노트북의 GitHub Desktop에서 다음 순서로 최신 코드를 받는다.

1. `Fetch origin`
2. 새 커밋이 표시되면 `Pull origin`

## 2. 토스페이먼츠

1. 사장님 본인 또는 정식 권한이 있는 대리인이 로그인한다.
2. 통신판매업 신고번호 또는 현재 진행 상태와 무료배송 기준만 확인한다.
3. 이용 신청 화면에 사업자등록증과 정산계좌를 직접 입력한다.
4. 가입비 결제와 전자계약은 확정 금액을 확인하고 별도 승인 후 진행한다.
5. 개발자센터에서 결제위젯 테스트 클라이언트 키와 테스트 시크릿 키를 확인한다.
6. 키는 복사해 Vercel에 직접 입력하고 다른 사람에게 전달하지 않는다.

## 3. Supabase

Supabase SQL Editor에서 아래 파일의 SQL을 실행한다.

`supabase/migrations/0004_orders_payments.sql`

이 마이그레이션은 다음을 만든다.

- 주문 테이블
- 주문상품 테이블
- 결제 테이블
- 회원 본인 및 관리자용 RLS 정책
- 주문 생성, 결제 중복 방지, 결제 상태 동기화 함수

실행 중 오류가 나면 반복 실행하거나 일부만 수정하지 말고 오류 화면만 공유한다.
비밀키나 고객정보가 포함된 화면은 공유하지 않는다.

## 4. Vercel 환경변수

프로젝트의 Vercel `Settings` → `Environment Variables`에서 아래 항목을
직접 입력한다. 실제 값은 GitHub에 저장하지 않는다.

| 환경변수 | 입력 내용 |
| --- | --- |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase 서버 전용 서비스 역할 키 |
| `TOSS_CLIENT_KEY` | 토스 결제위젯 테스트 클라이언트 키 |
| `TOSS_SECRET_KEY` | 토스 결제위젯 테스트 시크릿 키 |
| `SHIPPING_FEE_KRW` | 기본값 `3000`; 변경할 때만 입력 |
| `FREE_SHIPPING_THRESHOLD_KRW` | 사장님 확인값: 기준 금액 또는 무료배송이 없으면 `NONE` |
| `ALLOW_GUEST_CHECKOUT` | 기본값 `false`; 회원 구매만 허용 |
| `ALLOW_DELIVERY_MEMO` | 기본값 `true`; 선택형 메모와 직접 입력 제공 |
| `CHECKOUT_ENABLED` | 모든 설정과 테스트 전에는 `false` |
| `ALLOW_LIVE_PAYMENTS` | 라이브 전환 승인 전에는 `false` |

환경변수를 저장한 뒤 Vercel에서 새 배포가 필요하다.

## 5. 토스 웹훅

테스트 키와 주문 DB 설정이 끝난 뒤 토스페이먼츠 개발자센터의 웹훅 메뉴에
다음 주소를 등록한다.

`https://pi-coffee-roasters.vercel.app/api/toss/webhook`

등록 이벤트:

- `PAYMENT_STATUS_CHANGED`
- `DEPOSIT_CALLBACK`
- `CANCEL_STATUS_CHANGED`

## 6. 테스트 후 활성화

1. 먼저 테스트 키로 성공·실패·사용자 취소·새로고침·중복 승인 요청을 확인한다.
2. PC와 모바일에서 주문서와 결제 리다이렉트를 확인한다.
3. 테스트가 모두 끝나면 `CHECKOUT_ENABLED=true`로 바꾸고 다시 배포한다.
4. 카드사 심사와 계약 완료 전에는 라이브 키를 입력하지 않는다.
5. 라이브 키 전환 직전 사용자 최종 승인을 받고
   `ALLOW_LIVE_PAYMENTS=true`를 함께 적용한다.
